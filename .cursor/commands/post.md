# post

Push to Github

This command will:
1. Stage all changes
2. Commit with a descriptive message
3. Push to the main branch

Usage: `/post [optional commit message]`

If no commit message is provided, it will use a default message based on the changes.

